class Estudante extends Cliente {
    // 1 placa apenas
    double saldo; // pode ficar negativo
    // valor fixo por ingresso/dia, novo ingresso se sair após meia-noite
    // bloqueado se saldo < 0
}